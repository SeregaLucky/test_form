/**
 * Endpoints for backend requests
 */
const ENDPOINTS = {
  AUTH: {
    LOGIN: '/auth/login',
    FORGOT_PASSWORD: '/auth/password-reset',
    UPDATE_PASSWORD: '/auth/password-set',
  },
};

export default ENDPOINTS;
